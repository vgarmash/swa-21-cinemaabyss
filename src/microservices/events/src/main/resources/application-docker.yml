spring:
  cloud:
    stream:
      kafka:
        binder:
          brokers: kafka:9092
          auto-create-topics: true

logging:
  level:
    com.cinemaabyss.eventsservice: DEBUG
    org.apache.kafka: WARN